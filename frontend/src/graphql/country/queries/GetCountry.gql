query Countries {
  countries {
    id
    name
    code
    emoji
    continent {
      id
      name
    }
  }
}

query Country($code: String!) {
  country(code: $code) {
    id
    code
    name
    emoji
    continent {
      id
      name
    }
  }
}
